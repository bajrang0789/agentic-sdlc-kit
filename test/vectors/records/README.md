# Trail Record vectors

The fixed genesis origin is SHA-256 of the exact UTF-8 text `groundtrail:trail-record-origin:v1`. Chain verification requires record zero, contiguous sequences, immediate digest links, and JCS self-digests. Checkpoint comparison validates exact supplied bytes only; it does not establish issuer or signature trust.
